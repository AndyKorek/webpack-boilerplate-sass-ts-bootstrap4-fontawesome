'use strict';

// Add Class on Hover
/*  class AddHover {
    constructor(target) {
        this.target = target;
        this.hover(this.target);
        console.log('new'+this.target);
    }

    hover(target) {
        let $hoverTarget = $(target);
        let rolloverClass = 'animated rubberBand';
        $hoverTarget.hover(
            (event) => {
                $(event.currentTarget).addClass(rolloverClass);
            },
            (event) => {
                $(event.currentTarget).removeClass(rolloverClass);
            }
        )
    }
}

new AddHover('.features');*/
